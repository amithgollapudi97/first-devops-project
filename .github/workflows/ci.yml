name: My First CI Pipeline

on:
  push:

jobs:
  run-python:
    runs-on: ubuntu-latest

    steps:
      - name: Get code
        uses: actions/checkout@v3

      - name: Run Python file
        run: python app.py
